/**
 * Returns random integers between from and to inclusive. from and to can be negative.
 */
export declare function randomInt(from: number, to: number): number;
/**
 * Real modulo operation that supports negative numbers.
 *
 * ```ts
 * mod(1, 3) // 1
 * mod(-1, 3) // 2
 * ```
 */
export declare function mod(x: number, y: number): number;
/**
 * Returns a promise that wait for ms milliseconds.
 */
export declare function waitMs(ms: number): Promise<unknown>;
